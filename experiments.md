# Experiments — lab notebook

Append-only. Read this at the start of every session (CLAUDE.md). One entry per
backtest run, using the protocol in CLAUDE.md.

---

_No backtest runs yet. The engine and its adversarial no-lookahead tests are in
place; no strategy has been written. The first entry here will be the first
backtest of the first strategy._
